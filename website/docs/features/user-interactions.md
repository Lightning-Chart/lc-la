---
title: User interactions
sidebar_position: 7
---

XY charts are interactive by default. Users can pan and zoom to inspect the data without application setup.

Data cursors are enabled by default. A cursor follows the pointer and shows the values at the inspected position.

The current API does not expose interaction events or interaction configuration.
